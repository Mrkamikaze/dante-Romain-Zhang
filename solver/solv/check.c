/*
** EPITECH PROJECT, 2023
** dante
** File description:
** for diffrent check
*/

#include "map.h"

/*
int is_position(map_t *map, int x, int y)
{
    if (x < 0 || x >= map->height)
        return 0;
    if (y < 0 || y >= map->width)
        return 0;
    if (map -> map[x][y] != 1)
        return 0;
    return 1;
}
*/
